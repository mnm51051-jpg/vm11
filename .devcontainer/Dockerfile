FROM mcr.microsoft.com/devcontainers/base:ubuntu

# Minimal Dockerfile kept as a build context for the devcontainer.
# The devcontainer.json features will provide Docker-in-Docker and the Docker CLI.
